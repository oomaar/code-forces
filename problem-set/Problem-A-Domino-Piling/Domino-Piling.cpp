#include <iostream>
#include <string>

using namespace std;

int main() {
    int length;
    int width;
    
    cin >> length;
    cin >> width;
    
    int result = length * width;
    
    cout << result / 2;
}